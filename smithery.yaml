# Smithery.ai MCP Server Configuration
# https://smithery.ai/docs/config

startCommand:
  type: stdio
  config:
    command: node
    args:
      - build/index.js

build:
  command: npm install && npm run build
