# Smithery.ai MCP Server Configuration
# https://smithery.ai/docs/config

startCommand:
  type: stdio
  config:
    command: node
    args:
      - mcp-server/build/index.js

build:
  command: npm --prefix mcp-server install && npm --prefix mcp-server run build
