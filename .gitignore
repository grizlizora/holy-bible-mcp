# Dependencies
node_modules/
venv/
.venv/

# Database raw files & temporary extraction
data/raw/
*.zip
*.tar.gz
*.tmp

# SQLite Database (Too large for direct git tracking; hosted on HuggingFace/Releases)
data/processed/*.sqlite
data/processed/*.sqlite-wal
data/processed/*.sqlite-shm

# Build output & logs (keep mcp-server/build tracked for npx)
*.log
.DS_Store

# Environment Secrets
.env
*.env

# Local Telegram Bot (Kept locally on device, not pushed to MCP repo)
telegram-bot/

# Large DB & Processed files
data/processed/*.duckdb
*.duckdb
